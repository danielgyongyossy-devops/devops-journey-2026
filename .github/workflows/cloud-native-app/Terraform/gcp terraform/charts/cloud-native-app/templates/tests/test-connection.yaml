apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloud-native-app.fullname" . }}-test-connection"
  labels:
    {{- include "cloud-native-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloud-native-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
